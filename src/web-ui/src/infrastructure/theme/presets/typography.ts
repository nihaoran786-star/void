import { DEFAULT_UI_FONT_FAMILY } from '@/shared/constants/typography';

/**
 * All built-in themes share the same UI family. Custom themes may still
 * provide their own family through ThemeConfig.
 */
export const BUILTIN_THEME_UI_FONT_FAMILY = DEFAULT_UI_FONT_FAMILY;
