# Issue 2: CLI And ACP Low-Risk Upstream Polish

## What to build

Migrate the upstream CLI substring slash-command matching and add the `omp`
native ACP preset with void-safe copy and installer behavior.

## Acceptance criteria

- [ ] CLI slash command menu matches command substrings case-insensitively.
- [ ] Existing prefix and exact command matching still works.
- [ ] `omp` is available as a native ACP preset.
- [ ] `omp` is marked user-managed with no one-click npm installer.
- [ ] Comments and user-facing copy use void naming, not legacy upstream naming.

## Blocked by

None - can start immediately.
